# Pagy configuration
# See https://github.com/ddnexus/pagy
# In Pagy 9+, DEFAULT is frozen — configure at class level before first use.
require "pagy"

# Pagy::DEFAULT[:limit] is already 20 by default — only customise if needed.
# Pagy::DEFAULT[:limit] = 20
