<%#
  Shared layout variables and styles
  Use this partial in any view that needs common styling:
  render "shared/layout_vars"
  Then use: background: var(--bg-main); color: var(--color-primary);
%>
<%
  # CSS custom properties available for all views
  # Set in application.html.erb head
%>