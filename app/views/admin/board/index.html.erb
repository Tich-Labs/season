<% content_for(:page_title) { "Trello Board — Bug Tracker" } %>

<%# ── Legend ── %>
<div class="mb-6 flex flex-wrap gap-2 text-xs font-semibold">
  <span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full bg-green-100 text-green-700">&#10003; Fixed</span>
  <span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full bg-blue-100 text-blue-700">&#9654; In Progress</span>
  <span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full bg-red-100 text-red-700">&#9675; Open</span>
  <span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full bg-gray-100 text-gray-500">&#9723; Wishlist</span>
  <span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full bg-purple-100 text-purple-700">&#9654;&#9654; Previously Done</span>
</div>

<%# ═══════════════════════════════════════════════════════════════════════════
    SECTION 1 — FIXED IN THIS SESSION (2026-06-23)
    ═══════════════════════════════════════════════════════════════════════════ %>
<div class="mb-8">
  <h2 class="text-base font-bold text-green-700 mb-3 flex items-center gap-2">
    <span class="inline-block w-2 h-2 rounded-full bg-green-500"></span>
    Fixed — Session 2026-06-23
  </h2>

  <%# Card helper: each fix is one card %>

  <%# ── #13 / #23 ── %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#13</span>
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#23</span>
          <span class="font-semibold text-gray-800">Too much padding at the top / pages starting too far down</span>
          <span class="text-xs bg-yellow-100 text-yellow-700 px-2 py-0.5 rounded-full font-medium">Testflight + Web</span>
        </div>
        <p class="text-xs text-gray-500">Raised: 27 Apr 2026 (#13) &nbsp;&bull;&nbsp; 28 May 2026 (#23) &nbsp;&bull;&nbsp; Fixed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li>Main layout <code class="bg-gray-100 px-1 rounded">padding-top</code> reduced: <code class="bg-gray-100 px-1 rounded">6rem → 5rem</code> (removes 16px dead space below header)</li>
          <li>Onboarding outer div base padding: <code class="bg-gray-100 px-1 rounded">60px → 20px</code></li>
          <li>Native CSS override strips onboarding base padding entirely in the iOS shell — safe-area inset handles it</li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>Open the app on web browser and navigate to Calendar</li>
          <li>Confirm content starts immediately below the header bar with a small gap (~4px) — not a large white strip</li>
          <li>On iOS Testflight, open the Onboarding steps 1–11</li>
          <li>Confirm the first heading is visible near the top of the screen without excessive blank space above</li>
        </ol>
        <p class="mt-2 text-xs text-green-600 font-medium">&#10003; Expected: content sits closer to the top bar; no large blank band</p>
      </div>
    </div>
  </div>

  <%# ── #24 ── %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#24</span>
          <span class="font-semibold text-gray-800">Onboarding — birth control & food preferences can't be selected; only "None" submits</span>
          <span class="text-xs bg-yellow-100 text-yellow-700 px-2 py-0.5 rounded-full font-medium">Testflight</span>
        </div>
        <p class="text-xs text-gray-500">Raised: 28 May 2026 &nbsp;&bull;&nbsp; Fixed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li>Onboarding outer container changed from <code class="bg-gray-100 px-1 rounded">min-h-dvh</code> to <code class="bg-gray-100 px-1 rounded">h-dvh overflow-y-auto</code> — all options are now reachable by scrolling</li>
          <li>"None" added as the first option in Step 9 (birth control) — previously there was no way to skip</li>
          <li>Fixed locale-independent sentinel value so German locale also works correctly</li>
          <li>Controller updated to store <code class="bg-gray-100 px-1 rounded">nil</code> for "None" selection</li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>Create a new test account and go through onboarding</li>
          <li>On Step 9 (birth control method), scroll down — all options including "None" should be visible and tappable</li>
          <li>Select "IUD" (or any non-None option) and tap it — it should advance to Step 10</li>
          <li>On Step 11 (food preferences), scroll and select "Vegan" — should advance to Step 11</li>
          <li>Select "None" — should also advance correctly</li>
        </ol>
        <p class="mt-2 text-xs text-green-600 font-medium">&#10003; Expected: every option is tappable; selecting any option advances the flow</p>
      </div>
    </div>
  </div>

  <%# ── #10 ── %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#10</span>
          <span class="font-semibold text-gray-800">"Give feedback" button should only appear in burger menu, not in Track My Day</span>
        </div>
        <p class="text-xs text-gray-500">Raised: 28 Apr 2026 &nbsp;&bull;&nbsp; Fixed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li>Weekly feedback nudge banner removed from <code class="bg-gray-100 px-1 rounded">tracking/index.html.erb</code></li>
          <li>Feedback remains accessible in the burger/hamburger menu only</li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>Log in and navigate to the Tracking tab (Track My Day)</li>
          <li>Scroll through the full page — confirm no "Give feedback" or "Week X feedback" banner appears</li>
          <li>Open the burger menu (☰) — confirm "Feedback" is still listed there</li>
        </ol>
        <p class="mt-2 text-xs text-green-600 font-medium">&#10003; Expected: no feedback button on the tracking page; still present in burger menu</p>
      </div>
    </div>
  </div>

  <%# ── #21 ── %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#21</span>
          <span class="font-semibold text-gray-800">Mood selection — selected mood should be full opacity; others fade; deselect removes opacity</span>
        </div>
        <p class="text-xs text-gray-500">Raised: 5 May 2026 &nbsp;&bull;&nbsp; Verified: 23 Jun 2026 (already wired correctly — no code change needed)</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li>No code change — the logic was already implemented in <code class="bg-gray-100 px-1 rounded">symptom_controller.js</code></li>
          <li>Selected mood: opacity 1, scale 1.08, no grayscale</li>
          <li>Unselected moods: opacity 0.35, grayscale 0.6</li>
          <li>Server-side selected state read from <code class="bg-gray-100 px-1 rounded">data-active-moods</code> on connect</li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>Navigate to Tracking → Symptoms</li>
          <li>Open the Mood section</li>
          <li>Tap a mood icon — it should stay full opacity; all others should fade and show grayscale</li>
          <li>Tap the same icon again — it should deselect; all icons return to faded state</li>
          <li>Tap two different moods — both should remain full opacity</li>
        </ol>
        <p class="mt-2 text-xs text-green-600 font-medium">&#10003; Expected: visual toggle works correctly on every tap</p>
      </div>
    </div>
  </div>

  <%# ── #20 ── %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#20</span>
          <span class="font-semibold text-gray-800">Period end date cannot be added</span>
        </div>
        <p class="text-xs text-gray-500">Raised: 4 May 2026 &nbsp;&bull;&nbsp; Fixed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li>Period tracking view now shows two buttons: <strong>Set as period start</strong> and <strong>Set as period end</strong></li>
          <li>Selected date is displayed above the buttons for clarity</li>
          <li>Controller now saves to <code class="bg-gray-100 px-1 rounded">last_period_end</code> (DB column already existed)</li>
          <li>Calendar highlights real end date instead of hardcoded +4 days</li>
          <li>Current start and end dates shown below the buttons if set</li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>Go to Tracking → Track my Period</li>
          <li>Tap a past date in the calendar (e.g. the 1st of the month)</li>
          <li>Confirm the date shows as "Selected: 1 June" below the calendar</li>
          <li>Tap <strong>"Set as period start"</strong> — confirm success notice and redirect to Tracking home</li>
          <li>Return to Track my Period, tap the 5th of the month</li>
          <li>Tap <strong>"Set as period end"</strong> — confirm success notice stays on the period page</li>
          <li>Confirm the calendar highlights days 1–5 in the period colour</li>
        </ol>
        <p class="mt-2 text-xs text-green-600 font-medium">&#10003; Expected: both start and end can be set independently; calendar reflects the actual range</p>
      </div>
    </div>
  </div>

  <%# ── #19 ── %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#19</span>
          <span class="font-semibold text-gray-800">Symmetry is off — text overflows row, should wrap</span>
        </div>
        <p class="text-xs text-gray-500">Raised: 4 May 2026 &nbsp;&bull;&nbsp; Fixed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li>Tracking nav rows (Symptoms, Superpower, Period): fixed height <code class="bg-gray-100 px-1 rounded">h-12</code> replaced with <code class="bg-gray-100 px-1 rounded">min-h-[3rem] py-3</code> so rows expand if text wraps</li>
          <li>Text spans given <code class="bg-gray-100 px-1 rounded">flex-1 min-w-0</code> to prevent overflowing the arrow icon</li>
          <li>Bonus: removed hardcoded <code class="bg-gray-100 px-1 rounded"> P.M.</code> suffix from appointment time display (was showing on all appointments regardless of AM/PM)</li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>Go to Tracking — check that "My Symptoms", "My Superpower", "Track my Period" rows all align correctly with the arrow on the right</li>
          <li>Check the rows on a narrow screen (375px width) — text should wrap inside the row, not push the arrow off-screen</li>
          <li>Navigate to Calendar → Appointments — create an event with a start time of 09:00</li>
          <li>Confirm the time shows as "9:00" (not "9:00 P.M.")</li>
        </ol>
        <p class="mt-2 text-xs text-green-600 font-medium">&#10003; Expected: rows stay symmetrical; appointment times display without the incorrect "P.M." suffix</p>
      </div>
    </div>
  </div>
</div>

<%# ═══════════════════════════════════════════════════════════════════════════
    SECTION 1b — CODE REVIEW FIXES (2026-06-23, session 2)
    These were found by automated code review after the Trello fixes were pushed.
    ═══════════════════════════════════════════════════════════════════════════ %>
<div class="mb-8">
  <h2 class="text-base font-bold text-green-700 mb-3 flex items-center gap-2">
    <span class="inline-block w-2 h-2 rounded-full bg-green-500"></span>
    Code Review Fixes — 23 Jun 2026 (Session 2)
  </h2>

  <%# calendar_toggle regression %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">CR-1</span>
          <span class="font-semibold text-gray-800">Calendar settings toggles completely broken after dead-JS cleanup</span>
          <span class="text-xs bg-red-100 text-red-700 px-2 py-0.5 rounded-full font-medium">Regression</span>
        </div>
        <p class="text-xs text-gray-500">Found: 23 Jun 2026 &nbsp;&bull;&nbsp; Fixed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li><code class="bg-gray-100 px-1 rounded">calendar_toggle_controller.js</code> was deleted as "dead JS" but <code class="bg-gray-100 px-1 rounded">settings/calendar.html.erb</code> still wired all 5 toggle buttons to it via <code class="bg-gray-100 px-1 rounded">data-action="click->calendar-toggle#toggle"</code></li>
          <li>Controller recreated with <code class="bg-gray-100 px-1 rounded">toggle()</code> and <code class="bg-gray-100 px-1 rounded">submitForm()</code> actions — clicking any toggle now updates the hidden input and auto-submits the form</li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>Go to Settings → My Calendar</li>
          <li>Toggle "Appointments" off — page should reload and the toggle should remain off</li>
          <li>Toggle "Moon Phases" on — page should reload and show it as active</li>
          <li>Change the "Day of the Week" select to Sunday — page should auto-save</li>
        </ol>
        <p class="mt-2 text-xs text-green-600 font-medium">&#10003; Expected: all 5 toggles save immediately; week-start select auto-saves on change</p>
      </div>
    </div>
  </div>

  <%# Onboarding resume loop %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">CR-2</span>
          <span class="font-semibold text-gray-800">Selecting "None" for birth control sends users back to onboarding step 9 on every login</span>
          <span class="text-xs bg-red-100 text-red-700 px-2 py-0.5 rounded-full font-medium">Logic Bug</span>
        </div>
        <p class="text-xs text-gray-500">Found: 23 Jun 2026 &nbsp;&bull;&nbsp; Fixed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li>Step 9 view was submitting a fixed <code class="bg-gray-100 px-1 rounded">"none"</code> sentinel for the "None" option regardless of locale — controller stored <code class="bg-gray-100 px-1 rounded">nil</code></li>
          <li>The <code class="bg-gray-100 px-1 rounded">ALL_ONBOARDING_STEPS</code> step 9 check in <code class="bg-gray-100 px-1 rounded">user.rb</code> treats <code class="bg-gray-100 px-1 rounded">nil</code> as incomplete — users looped back to step 9 on every login</li>
          <li>Fix: view now submits the actual label ("None", "Keine") instead of the sentinel; controller stores the label directly; the DB default <code class="bg-gray-100 px-1 rounded">"none"</code> (lowercase) remains distinct, preserving the resume logic</li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>Create a new test account and complete onboarding, selecting "None" on step 9</li>
          <li>Sign out and sign back in</li>
          <li>Confirm you land on the Calendar, not step 9 of onboarding</li>
          <li>Repeat in German locale — selecting "Keine" should also complete step 9</li>
        </ol>
        <p class="mt-2 text-xs text-green-600 font-medium">&#10003; Expected: "None" completes step 9; no redirect back to onboarding on subsequent logins</p>
      </div>
    </div>
  </div>

  <%# Date.parse no rescue + end<start + asymmetric redirect %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">CR-3</span>
          <span class="font-semibold text-gray-800">Period tracking controller — bad date crashes app; end before start accepted; redirect asymmetry</span>
          <span class="text-xs bg-orange-100 text-orange-700 px-2 py-0.5 rounded-full font-medium">3 bugs in 1</span>
        </div>
        <p class="text-xs text-gray-500">Found: 23 Jun 2026 &nbsp;&bull;&nbsp; Fixed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li>All 4 <code class="bg-gray-100 px-1 rounded">Date.parse</code> calls replaced with <code class="bg-gray-100 px-1 rounded">Date.iso8601</code> inside <code class="bg-gray-100 px-1 rounded">rescue ArgumentError, TypeError</code> — malformed dates now redirect with a flash error instead of 500</li>
          <li>Guard added: end date is rejected if it precedes the current period start date</li>
          <li>Both "Set as period start" and "Set as period end" now redirect to the period page so users can set both in sequence without extra navigation</li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>On the period page, tap a date and tap "Set as period start" — confirm redirect stays on the period page with a success notice</li>
          <li>Tap an earlier date and tap "Set as period end" — confirm a red alert: "End date cannot be before the start date"</li>
          <li>Tap a later date and tap "Set as period end" — confirm success notice and stays on the period page</li>
          <li>As a sanity check, manually visit <code class="bg-gray-100 px-1 rounded">/tracking/period?save=1&date=notadate</code> — confirm redirect with error, not a 500</li>
        </ol>
        <p class="mt-2 text-xs text-green-600 font-medium">&#10003; Expected: bad dates are rejected gracefully; end before start is rejected; both saves stay on the period page</p>
      </div>
    </div>
  </div>

  <%# Cross-month period band + fabricated fallback %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">CR-4</span>
          <span class="font-semibold text-gray-800">Period calendar: cross-month days invisible; fake 5-day band shown when no end date set</span>
        </div>
        <p class="text-xs text-gray-500">Found: 23 Jun 2026 &nbsp;&bull;&nbsp; Fixed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li>Removed the <code class="bg-gray-100 px-1 rounded">period_start + 4.days</code> fallback — no more fabricated pink band for users who haven't set a period end</li>
          <li>Period day calculation now handles cross-month ranges: if start is in a prior month and end extends into the current month, the correct days in the current month are highlighted</li>
          <li>If only a start date is set (no end), highlighting runs from start to today within the month</li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>Set period start to the last day of the previous month (e.g. 30 May) and end to the 4th of this month (4 Jun)</li>
          <li>Navigate to the period page showing June — confirm days 1–4 are highlighted in red</li>
          <li>Create a fresh test account with no period end date set — confirm no pink band appears on the calendar</li>
        </ol>
        <p class="mt-2 text-xs text-green-600 font-medium">&#10003; Expected: cross-month periods highlight correctly; no band when no end date set</p>
      </div>
    </div>
  </div>

</div>

<%# ═══════════════════════════════════════════════════════════════════════════
    SECTION 2 — PREVIOUSLY COMPLETED (before this session)
    ═══════════════════════════════════════════════════════════════════════════ %>
<div class="mb-8">
  <h2 class="text-base font-bold text-purple-700 mb-3 flex items-center gap-2">
    <span class="inline-block w-2 h-2 rounded-full bg-purple-500"></span>
    Previously Completed
  </h2>

  <div class="bg-white rounded-xl border border-gray-200 overflow-hidden divide-y divide-gray-100">
    <% [
      { id: "#5",  title: '"Forgot password" does not work, no email received', raised: "22 Apr 2026", fixed: "27 Apr 2026" },
      { id: "#8",  title: "When trying to reset password, no email received",   raised: "22 Apr 2026", fixed: "27 Apr 2026" },
      { id: "#16", title: '"Information" displaced — should be top-right icon, not burger menu', raised: "prior", fixed: "16 Jun 2026" },
    ].each do |t| %>
      <div class="flex items-center gap-4 px-5 py-3">
        <span class="inline-flex items-center justify-center w-6 h-6 rounded-full bg-purple-100 text-purple-700 text-xs font-bold shrink-0">&#10003;</span>
        <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded"><%= t[:id] %></span>
        <span class="flex-1 text-sm text-gray-700"><%= t[:title] %></span>
        <span class="text-xs text-gray-400 shrink-0">Raised <%= t[:raised] %> &bull; Fixed <%= t[:fixed] %></span>
      </div>
    <% end %>
  </div>
</div>

<%# ═══════════════════════════════════════════════════════════════════════════
    SECTION 3 — STILL OPEN
    ═══════════════════════════════════════════════════════════════════════════ %>
<div class="mb-8">
  <h2 class="text-base font-bold text-red-700 mb-3 flex items-center gap-2">
    <span class="inline-block w-2 h-2 rounded-full bg-red-400"></span>
    Still Open — To Do
  </h2>

  <div class="bg-white rounded-xl border border-gray-200 overflow-hidden divide-y divide-gray-100">
    <% [
      { id: "#2",  raised: "22 Apr 2026", title: "Login with Apple / Facebook / Google not working", note: "Ops: code is complete. Need to verify APPLE_CLIENT_ID, GOOGLE_CLIENT_ID/SECRET, FACEBOOK_APP_ID/SECRET env vars on Render, and update OAuth redirect URIs in each provider console to match the production URL." },
      { id: "#6",  raised: "22 Apr 2026", title: "Create new account is not working", note: "Ops: Resend delivery is wired (production.rb + resend.rb). Test: (1) Render → Environment → confirm RESEND_API_KEY matches active key in Resend dashboard. (2) Confirm MAIL_FROM domain (season.vision) is verified as a sender in Resend. (3) Confirm APP_HOST is set to the correct production URL. (4) Register a fresh email on web → check inbox + spam for confirmation email. (5) Click confirmation link → should auto-login and redirect to onboarding." },
    ].each do |t| %>
      <div class="flex items-start gap-4 px-5 py-3">
        <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-red-100 text-red-400 text-xs font-bold shrink-0">&#9675;</span>
        <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded mt-0.5 shrink-0"><%= t[:id] %></span>
        <div class="flex-1 min-w-0">
          <p class="text-sm font-medium text-gray-800"><%= t[:title] %></p>
          <p class="text-xs text-gray-400 mt-0.5"><%= t[:note] %></p>
        </div>
        <span class="text-xs text-gray-400 shrink-0 mt-0.5">Raised <%= t[:raised] %></span>
      </div>
    <% end %>
  </div>
</div>

<%# ═══════════════════════════════════════════════════════════════════════════
    SECTION 4 — WISHLIST FIXED (2026-06-23, session 3)
    ═══════════════════════════════════════════════════════════════════════════ %>
<div class="mb-8">
  <h2 class="text-base font-bold text-green-700 mb-3 flex items-center gap-2">
    <span class="inline-block w-2 h-2 rounded-full bg-green-500"></span>
    Wishlist Fixed — 23 Jun 2026 (Session 3)
  </h2>

  <%# #3 — Opening screen spacing %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#3</span>
          <span class="font-semibold text-gray-800">Opening screen — reduce space above logo for a more balanced look</span>
        </div>
        <p class="text-xs text-gray-500">Raised: 22 Apr 2026 &nbsp;&bull;&nbsp; Fixed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li>Welcome screen <code class="bg-gray-100 px-1 rounded">padding-top</code> reduced from <code class="bg-gray-100 px-1 rounded">60px</code> to <code class="bg-gray-100 px-1 rounded">20px</code> (safe-area inset still applied for iOS notch)</li>
          <li>Removed redundant <code class="bg-gray-100 px-1 rounded">min-h-screen</code> class alongside <code class="bg-gray-100 px-1 rounded">min-h-dvh</code></li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>Open the app root URL (<code class="bg-gray-100 px-1 rounded">/welcome</code>) when logged out</li>
          <li>Confirm the Season logo sits closer to the vertical centre of the screen — not pushed down by a large blank gap at the top</li>
          <li>On iOS Testflight, confirm the logo is not cut off by the notch (safe-area inset still applies)</li>
        </ol>
        <p class="mt-2 text-xs text-green-600 font-medium">&#10003; Expected: logo and wordmark centred with balanced whitespace above and below</p>
      </div>
    </div>
  </div>

  <%# #4 — Registration spacing + field consistency %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#4</span>
          <span class="font-semibold text-gray-800">Registration — reduce spacing; repeat password needs matching background</span>
        </div>
        <p class="text-xs text-gray-500">Raised: 22 Apr 2026 &nbsp;&bull;&nbsp; Fixed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li>Top padding reduced 60px → 20px (same as welcome screen fix)</li>
          <li>Subtitle gap <code class="bg-gray-100 px-1 rounded">mt-11</code> → <code class="bg-gray-100 px-1 rounded">mt-6</code>; social login <code class="bg-gray-100 px-1 rounded">mt-[62px]</code> → <code class="bg-gray-100 px-1 rounded">mt-8</code>; "or email" divider <code class="bg-gray-100 px-1 rounded">mt-[62px]</code> → <code class="bg-gray-100 px-1 rounded">mt-6</code>; submit button <code class="bg-gray-100 px-1 rounded">mt-[75px]</code> → <code class="bg-gray-100 px-1 rounded">mt-8</code></li>
          <li>Repeat password field: added <code class="bg-gray-100 px-1 rounded">opacity-50</code> in normal state to match email and password fields; error state now uses <code class="bg-gray-100 px-1 rounded">bg-brand-error</code> instead of a hardcoded hex</li>
          <li>Note: the eye icon on repeat password was already built — Trello ticket was filed before it existed</li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>Open <code class="bg-gray-100 px-1 rounded">/registration/new</code> on a 375px-wide screen (iPhone SE size)</li>
          <li>Confirm all three fields (email, password, repeat password), the social login buttons, and the "Create account" button are all visible without scrolling (or with minimal scroll)</li>
          <li>Confirm email, password and repeat password fields look identical in their unfocused state — same tinted field background with matching opacity</li>
          <li>Tap the eye icon on the repeat password field — confirm the password text toggles visible/hidden</li>
          <li>Submit with mismatched passwords — confirm the repeat password field turns the error background colour</li>
        </ol>
        <p class="mt-2 text-xs text-green-600 font-medium">&#10003; Expected: tighter layout, all three fields visually identical, eye icon works on both password fields</p>
      </div>
    </div>
  </div>

</div>

<%# ── Quick Wins — 23 Jun 2026 (Session 4) ── %>
<div class="mb-8">
  <h2 class="text-xl font-bold text-gray-800 mb-4">Quick Wins Fixed — 23 Jun 2026 (Session 4)</h2>

  <%# #14 — Forecast missing nav link %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#14</span>
          <span class="font-semibold text-gray-800">Forecast — no navigation entry in burger menu</span>
        </div>
        <p class="text-xs text-gray-500">Raised: Low priority &nbsp;&bull;&nbsp; Fixed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li>Added "Today's Forecast" link to the burger menu at position top-[461px] (after Monthly-View)</li>
          <li>Shifted all menu items below it down 40px to avoid overlap</li>
          <li>Added <code class="bg-gray-100 px-1 rounded">nav.menu.forecast</code> locale key to en.yml and de.yml</li>
          <li>Note: every calendar day cell already linked to forecast — this adds a direct entry point for today's forecast</li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>Open the app and tap the hamburger (☰) menu</li>
          <li>Confirm "Today's Forecast" appears between Monthly-View and the second divider</li>
          <li>Tap it — confirm it navigates to <code class="bg-gray-100 px-1 rounded">/forecast</code> showing today's forecast</li>
          <li>Also confirm tapping any day cell on the monthly calendar still navigates to the forecast for that date</li>
          <li>Confirm menu items below (Settings, Feedback, Bug Report, Log out) are all visible and not overlapping</li>
        </ol>
        <p class="mt-2 text-xs text-green-600 font-medium">&#10003; Expected: "Today's Forecast" visible in burger menu, navigates correctly</p>
      </div>
    </div>
  </div>

  <%# #15 — Onboarding period start/end broken %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#15</span>
          <span class="font-semibold text-gray-800">Onboarding steps 3 &amp; 4 — period start/end date selection broken</span>
        </div>
        <p class="text-xs text-gray-500">Raised: Low priority &nbsp;&bull;&nbsp; Fixed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li>Recreated <code class="bg-gray-100 px-1 rounded">onboarding_calendar_controller.js</code> — it was incorrectly deleted as "dead JS" in a prior cleanup session</li>
          <li>Steps 3 and 4 both use <code class="bg-gray-100 px-1 rounded">data-controller="onboarding-calendar"</code> — without the controller, day taps did nothing and the submit button stayed hidden</li>
          <li>Controller handles: day tap → highlight selected day (inline style) + populate hidden field + show submit/clear buttons; clear tap → reset all</li>
          <li>Private methods <code class="bg-gray-100 px-1 rounded">#applyHighlight</code> / <code class="bg-gray-100 px-1 rounded">#clearHighlight</code> use inline style to avoid Tailwind class conflicts</li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>Sign up a fresh test account and start onboarding</li>
          <li>Reach step 3 (period start date) — tap any past date on the calendar</li>
          <li>Confirm the selected date highlights in the brand-primary colour and the "Set period start" submit button appears below the calendar</li>
          <li>Tap the "Clear" link — confirm highlight clears and submit button hides again</li>
          <li>Select a date and submit — confirm you advance to step 4</li>
          <li>Repeat steps 2–5 for step 4 (period end date)</li>
          <li>Confirm the "I don't know" button on both steps still works independently</li>
        </ol>
        <p class="mt-2 text-xs text-green-600 font-medium">&#10003; Expected: day tap highlights date and shows submit button; form submits correctly; clear resets state</p>
      </div>
    </div>
  </div>

  <%# #25 — Onboarding calendar (weekdays added, controller restored, Unsure works) %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#25</span>
          <span class="font-semibold text-gray-800">*Testflight* No weekdays above calendar; date selection broken; Unsure button not working</span>
        </div>
        <p class="text-xs text-gray-500">Raised: 28 May 2026 &nbsp;&bull;&nbsp; Fixed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li><strong>Weekdays missing</strong>: added Mo–Su header row above the calendar grid in both onboarding step 3 and step 4</li>
          <li><strong>Date selection broken</strong>: <code class="bg-gray-100 px-1 rounded">onboarding_calendar_controller.js</code> was deleted — recreated this session with <code class="bg-gray-100 px-1 rounded">select</code> and <code class="bg-gray-100 px-1 rounded">clear</code> actions, inline-style highlights for selected day</li>
          <li><strong>Unsure button</strong>: it's in its own separate form (submits <code class="bg-gray-100 px-1 rounded">skip_last_period: "1"</code>); it was never broken by JS — the button works independently of the calendar controller</li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>New onboarding step 3: Mo–Su row appears above the calendar grid</li>
          <li>Tap a past date — it highlights in brand red, Submit button appears</li>
          <li>Tap another date — previous clears, new one highlights</li>
          <li>Tap Clear link — highlight removed, Submit hidden again</li>
          <li>Tap Unsure — advances to step 4 (separate form, no JS needed)</li>
          <li>Repeat steps 1–5 on step 4</li>
        </ol>
      </div>
    </div>
  </div>

  <%# #22 — Day rotator swipe (onboarding-picker controller restored) %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#22</span>
          <span class="font-semibold text-gray-800">*Testflight* Day rotator (cycle length) only works by clicking, not swipe-scrollable</span>
        </div>
        <p class="text-xs text-gray-500">Raised: 28 May 2026 &nbsp;&bull;&nbsp; Fixed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li>Root cause: <code class="bg-gray-100 px-1 rounded">onboarding_picker_controller.js</code> was deleted in a prior dead-JS cleanup but onboarding step 6 still references <code class="bg-gray-100 px-1 rounded">data-controller="onboarding-picker"</code></li>
          <li>Recreated controller: listens for <code class="bg-gray-100 px-1 rounded">scroll</code> with 80ms debounce (fires after snap settles on iOS)</li>
          <li>On scroll settle: highlights centred item (opacity 1, font-weight 600) and writes its <code class="bg-gray-100 px-1 rounded">data-days</code> value to the hidden input</li>
          <li>Click handler: scrolls to tapped item then commits</li>
          <li>Keyboard: arrow keys move selection</li>
          <li>On connect: scrolls to existing value (default 28) without animation</li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>New onboarding: reach step 6 (cycle length)</li>
          <li>Swipe the scroll drum up and down — it snaps to items and the centred day highlights (bold, full opacity)</li>
          <li>Tap a day directly — drum scrolls to it and it highlights</li>
          <li>Tap Submit — saved cycle length matches what was centred (check in admin user detail)</li>
          <li>Verify on Testflight (iOS native) that touch-swipe works end-to-end</li>
        </ol>
      </div>
    </div>
  </div>

  <%# #12 — Symptoms (fully built; Figma content audit pending) %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-yellow-100 text-yellow-700 text-xs font-bold shrink-0">&#9651;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#12</span>
          <span class="font-semibold text-gray-800">Symptoms should match UX design; content missing</span>
          <span class="text-xs bg-yellow-100 text-yellow-700 px-2 py-0.5 rounded-full font-medium">Figma audit pending</span>
        </div>
        <p class="text-xs text-gray-500">Raised: 27 Apr 2026 &nbsp;&bull;&nbsp; Partially addressed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4">
      <p class="text-sm text-gray-700 mb-2">The symptoms page is fully functional: 22 moods, 24 physical symptoms, 8 mental symptoms, bleeding flow, vaginal discharge, sexual intercourse (6 options), cravings (5 options), sleep/temperature/weight vertical pickers, notes, and a pre-submit review modal. All six Stimulus controllers exist and are wired correctly.</p>
      <p class="text-sm text-gray-700">Remaining: a Figma audit to verify that symptom labels/icons match the design exactly. Fixed <code class="bg-gray-100 px-1 rounded">Date.parse</code> → <code class="bg-gray-100 px-1 rounded">Date.iso8601 + rescue</code> in <code class="bg-gray-100 px-1 rounded">SymptomsController#index</code>.</p>
    </div>
  </div>

  <%# #11 — Superpowers (slider UI built, show view fixed) %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#11</span>
          <span class="font-semibold text-gray-800">Superpowers — all superpowers need to be added + slider UI</span>
        </div>
        <p class="text-xs text-gray-500">Raised: 27 Apr 2026 &nbsp;&bull;&nbsp; Fixed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li>Slider UI was already built — 20 superpowers with 0–3 range, debounced auto-save via <code class="bg-gray-100 px-1 rounded">superpower_controller.js</code></li>
          <li>Fixed show view: was displaying <code class="bg-gray-100 px-1 rounded">/5</code> scale when scale is 0–3; fixed to show <code class="bg-gray-100 px-1 rounded">/3</code> and correct bar fill</li>
          <li>Fixed show view: emoji map used different superpower names — removed; names now display as-is</li>
          <li>Rewrote show view to use Tailwind classes (removed inline <code class="bg-gray-100 px-1 rounded">style=</code> throughout)</li>
          <li>Guarded <code class="bg-gray-100 px-1 rounded">Date.parse</code> in superpowers controller with <code class="bg-gray-100 px-1 rounded">Date.iso8601 + rescue</code></li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>Go to Tracking → My Superpower</li>
          <li>Move sliders — visual fills immediately, auto-saves after 500ms</li>
          <li>Tap Submit → returns to tracking index</li>
          <li>Return to Superpowers — recent log entry appears</li>
          <li>Tap a recent log entry → show view displays correct /3 scale and named bars</li>
        </ol>
      </div>
    </div>
  </div>

  <%# #9 — Period tracking (largely fixed by recent commits, hardened further) %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-green-100 text-green-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#9</span>
          <span class="font-semibold text-gray-800">Period tracking not working properly</span>
        </div>
        <p class="text-xs text-gray-500">Raised: 27 Apr 2026 &nbsp;&bull;&nbsp; Fixed: 23 Jun 2026</p>
      </div>
    </div>
    <div class="px-5 py-4 grid grid-cols-1 md:grid-cols-2 gap-4">
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">What changed</p>
        <ul class="text-sm text-gray-700 space-y-1 list-disc list-inside">
          <li>Onboarding steps 3 + 4: calendar controller restored (<code class="bg-gray-100 px-1 rounded">onboarding_calendar_controller.js</code> was deleted) — date tap and submit button now work</li>
          <li>Period save: guarded all <code class="bg-gray-100 px-1 rounded">Date.parse</code> calls with <code class="bg-gray-100 px-1 rounded">Date.iso8601 + rescue</code> in tracking, onboarding, and forecast controllers</li>
          <li>Period start redirect: now stays on the period page instead of bouncing to tracking index</li>
          <li>End date validation: rejects end &lt; start</li>
          <li>Calendar highlight: correctly shows cross-month period ranges; removed fabricated +4-day fallback</li>
        </ul>
      </div>
      <div>
        <p class="text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">Testing steps</p>
        <ol class="text-sm text-gray-700 space-y-1 list-decimal list-inside">
          <li>New user: complete onboarding, tap a date on step 3 — submit button appears, selection highlights</li>
          <li>Main app: go to Tracking → Track my Period → tap a date → tap "Set as period start" → confirm flash and calendar highlight</li>
          <li>Tracking page now shows your cycle phase and day strip</li>
          <li>Return to period page and set end date — confirm it only accepts dates ≥ start</li>
          <li>Forecast page (Today's Forecast from burger menu) shows phase-appropriate cards</li>
        </ol>
      </div>
    </div>
  </div>

  <%# #17 — Appointment modals (investigated, already working) %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-blue-100 text-blue-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#17</span>
          <span class="font-semibold text-gray-800">Appointment — setting time, add location, invite guest not working</span>
        </div>
        <p class="text-xs text-gray-500">Raised: 4 May 2026 &nbsp;&bull;&nbsp; Investigated: 23 Jun 2026 — ticket stale, feature was completed post-filing</p>
      </div>
    </div>
    <div class="px-5 py-4">
      <p class="text-sm text-gray-700 mb-2">Ticket was filed on 4 May when the appointment modals were still being built. Three subsequent commits (<code class="bg-gray-100 px-1 rounded">0072e4c</code>, <code class="bg-gray-100 px-1 rounded">d61e4f4</code>, Jun 10–17) completed all three Stimulus controllers:</p>
      <ul class="text-sm text-gray-700 list-disc list-inside space-y-1 mb-2">
        <li><code class="bg-gray-100 px-1 rounded">season_modal_controller.js</code> — location search (Nominatim), guests, reminder, repeat</li>
        <li><code class="bg-gray-100 px-1 rounded">appointment_date_picker_controller.js</code> — start/end date + time scroll picker modal</li>
        <li><code class="bg-gray-100 px-1 rounded">time_picker_controller.js</code> — inline hour/minute/AM-PM selects</li>
      </ul>
      <p class="text-sm text-gray-700">All form fields (location, guests, reminder_minutes, repeat_frequency, start_time, end_time) are in the DB schema, permitted in the controller, and wired to hidden inputs inside the form. Cleaned up the dead <code class="bg-gray-100 px-1 rounded">data-controller="form-persistence"</code> reference from <code class="bg-gray-100 px-1 rounded">calendar_events/new.html.erb</code> (that controller was deleted in a later cleanup commit).</p>
    </div>
  </div>

  <%# #18 — Calendar filter tabs (confirmed working) %>
  <div class="bg-white rounded-xl border border-gray-200 mb-4 overflow-hidden">
    <div class="flex items-start gap-4 px-5 py-4 border-b border-gray-100">
      <span class="mt-0.5 inline-flex items-center justify-center w-6 h-6 rounded-full bg-blue-100 text-blue-700 text-xs font-bold shrink-0">&#10003;</span>
      <div class="flex-1 min-w-0">
        <div class="flex flex-wrap items-center gap-2 mb-1">
          <span class="text-xs font-mono bg-gray-100 text-gray-600 px-1.5 py-0.5 rounded">#18</span>
          <span class="font-semibold text-gray-800">Calendar filter tabs (All / Events / Tracked / Cycle) — investigated, already working</span>
        </div>
        <p class="text-xs text-gray-500">Raised: Low priority &nbsp;&bull;&nbsp; Investigated: 23 Jun 2026 — no fix needed</p>
      </div>
    </div>
    <div class="px-5 py-4">
      <p class="text-sm text-gray-700">Code audit confirmed the full pipeline is correctly implemented: <code class="bg-gray-100 px-1 rounded">@mode</code> is read from params with a whitelist guard in <code class="bg-gray-100 px-1 rounded">CalendarController#index</code>; <code class="bg-gray-100 px-1 rounded">@tracked_days_set</code> is built as a Ruby Set via <code class="bg-gray-100 px-1 rounded">.pluck(:date).to_set</code>; the <code class="bg-gray-100 px-1 rounded">_day_cell</code> partial correctly uses <code class="bg-gray-100 px-1 rounded">mode</code> to toggle event labels, tracking dots, and phase bands. Filter tabs are plain GET links — no JS required. Ticket can be closed.</p>
    </div>
  </div>

</div>

<%# ── Footer ── %>
<p class="text-xs text-gray-400 mt-2">Source: Trello board "Feedback Web App" &bull; Last updated: 23 Jun 2026 (session 5 — all code tickets closed; #2 #6 are ops/env) &bull; <a href="https://trello.com" class="underline hover:text-gray-600" target="_blank">View on Trello</a></p>
