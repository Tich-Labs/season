# M4 — Forecast & Appointments: Trello Card Status

**Date:** 21 Aug 2026
**Milestone:** M4 (Forecasting and Appointments)
**Source:** 16 cards, Trello board "feedback-web-app"
**Deadline:** Happy 25.08.2026 · Realistic 02.09.2026 · Worst case 06.09.2026
**Status:** 🟡 In progress — 6 cards fixed, 1 parked, 4 open, 4 blocked on Figma/more info

---

## Summary

The Trello export listed 16 cards against M4 (Forecast & Appointments). A code-level audit against the running app found several already fixed by an earlier commit (`3dd4adb`, 16 Aug), several genuinely still broken, and a few that need Figma access this environment doesn't have. Two commits (`4ca6787`, `1816df1`) closed six of them.

---

## ✅ Fixed

| Card | Priority | Fix | Files |
|---|---|---|---|
| Burger menu: Today's Forecast not connected | P2 | Re-enabled the link (was dead-coded behind `if false`), wired the `search-status` icon, points at `forecast_path(tab: "forecast")` which now lands directly on the Forecast tab instead of Appointments | `_burger_menu.html.erb`, `forecast_tabs_controller.js`, `forecast/index.html.erb` |
| Create appointment: time field pop-up should be deleted | P1 | Removed the redundant inline hour/min/AM-PM editor on the time pill — editing now goes exclusively through the "Set date" modal. `time_picker_controller.js` deleted as orphaned | `calendar_events/new.html.erb`, `calendar_events/edit.html.erb`, `appointment_date_picker_controller.js` |
| Pop-up warning — Scenario 1 (period overlap) | P3 | The alert existed in JS but nothing triggered it. Added `CycleCalculatorService#period_ranges` and wired the dispatch | `cycle_calculator_service.rb`, `calendar_events_controller.rb`, `appointment_date_picker_controller.js` |
| Pop-up warning — Scenario 3 (unsaved changes) | P3 | Verified already working via `_turboBeforeVisit` in `season_modal_controller.js` (a separate `showUnsavedChangesAlert` method is dead code, never called — harmless) | — |
| Code-Fix: appointment can not be set / wrong dialogue | P1 | The wrong-dialogue-on-submit bug was already fixed pre-existing (`_submitting` guard). Updated the success copy to match what the card asked for | `config/locales/en.yml`, `de.yml` |
| Code-Fix: you can not leave the page | P1 | Verified already fixed — "< Back" (history-based) top-left, calendar/home FAB replacing the old "+" | — |

Also verified already fixed, pre-existing (no change needed): **Choose an icon padding/size**, **From/To alignment**, **Title line position** — all matched what the cards asked for as of the 16 Aug commit.

**Incidental fix along the way:** the burger-menu edit surfaced a stale-Tailwind-build gotcha — new arbitrary-value classes (`top-[564px]` etc.) don't render until `bin/rails tailwindcss:build` runs. Run `bin/dev` (which also runs `tailwindcss:watch`), not `bin/rails server` alone, while iterating on views.

---

## 🟡 Parked (user decision, 21 Aug 2026)

| Card | Priority | Status |
|---|---|---|
| Pop-up warning — Scenario 2 (past date) | P3 | Shipped a fix (`confirm()` re-entrant bypass + a real confirm/cancel flow in `showPastDateAlert`) that traces correctly reading the code, but in real testing the warning **never appears at all** — picking a past date just sets it silently. This is a functional-wiring bug, not a Figma/design gap, but shelved for now at the user's request. **If picked back up:** start by confirming `confirm()` is actually reached from the real UI click path before assuming a design-side cause — stage2's "Done" button is hardcoded to `confirmTime()` regardless of date-vs-time mode, worth double-checking that a date picked via scroll during `_mode: 'date'` correctly reaches `_pickYear/_pickMonth/_pickDay` by the time stage1's "Set date" button fires. |

---

## ⏳ Still open, untouched

| Card | Priority | Why not done |
|---|---|---|
| Code-Fix: repeat appointment is not working | P1 | Confirmed real: `repeat_frequency` is captured and saved but nothing anywhere expands it into actual occurrences — no recurrence logic in `CalendarEvent` or any controller/job. The "edit this vs. all future" modal in `edit.html.erb` (`recurringModal`/`openRecurring`) exists but is unwired. This is a feature build (data model + query changes across calendar/weekly/daily/forecast views), not a quick fix — needs to be scoped separately. |
| Top Bar is broken on every calendar view besides monthly | P1 | No static bug found reading the header/layout code across monthly/weekly/daily/appointments views — needs a live repro (browser-automation or device) before it can be diagnosed. |
| Creating appointment mistakes | P1 | No description in the Trello export to act on — needs the original card detail or attachment from the user. |
| UI-Fix: creating appointment padding is too big | P3 | The form is long (8 stacked sections); whether it should compress further to fit one screen is a Figma-height judgment call, not something to guess blind. |

---

## 🚧 Blocked on Figma access

No Figma MCP server is connected in this environment (checked via tool search — only a `claude.ai/design` design-systems tool is available, unrelated to Figma). `WebFetch` on Figma URLs returns only the login/app shell, not real design content.

| Card | Priority | Notes |
|---|---|---|
| Forecast: quality of the picture is wrong | P3 | Needs the SVG re-exported from Figma — an asset task, not a code fix. |
| M4 appointment view is wrong | P2 | Needs a side-by-side diff against the Figma milestone2 reference to know what's actually wrong. |
| Create appointment icon is wrong | P1 | Best lead found: the "+" trigger and every modal's close-X use a hardcoded `stroke="#A43600"` / `fill="#A43600"`, not brand primary `#933a35` — matches CLAUDE.md's "never hardcode colours not in Figma" rule, but could also be an intentional distinct icon shade in the actual Figma file. Unconfirmed without access. |

**To unblock:** connect a Figma MCP server (`/mcp` or `claude mcp add` in an interactive session), or supply exported screenshots/assets directly.

---

*Source Trello export: `M4-cards.md` (not committed to the repo). Commits: `4ca6787`, `1816df1`.*
